<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- ARCore requires CAMERA + an OpenGL ES 3.0 device -->
    <uses-feature android:name="android.hardware.camera.ar" android:required="false" />
    <uses-feature android:glEsVersion="0x00030000" android:required="true" />

    <!-- Cleartext ws:// allowed for LAN relay only — relay is designed for
         private networks and must never be exposed to the internet. -->
    <application
        android:name=".UnderstudyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Understudy"
        tools:targetApi="31">

        <!-- ARCore is optional: the app runs without it, falls back to non-AR UI -->
        <meta-data android:name="com.google.ar.core" android:value="optional" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|density|uiMode"
            android:theme="@style/Theme.Understudy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Companion teleprompter for AI Glasses (Android XR projected display).
             Launched from the phone via ProjectedContext.createProjectedActivityOptions;
             lives in the same process so GlassesTeleprompterState is shared. -->
        <activity
            android:name=".glasses.GlassesTeleprompterActivity"
            android:exported="true"
            android:requiredDisplayCategory="xr_projected"
            tools:targetApi="34"
            android:theme="@style/Theme.Understudy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="androidx.xr.projected.CATEGORY_PROJECTED" />
            </intent-filter>
        </activity>

        <!-- Share exported blocking JSON via content:// URI -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>
</manifest>
